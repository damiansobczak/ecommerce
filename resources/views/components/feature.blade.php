<div class="feature">
    <div class="feature__container">

        {{-- Feature product loop start --}}
        <div class="feature__item">
            <a href="{{ $link }}" class="feature__link">
                <div class="feature__price">{{ $price }}</div>
                <img src="{{ $image }}" alt="" class="feature__image">
                <div class="feature__title">{{ $title }}</div>
                <div class="feature__desc">{{ $desc }}</div>
                <div class="feature__more"><span></span> Więcej informacji</div>
            </a>
            <button class="feature__cart">Dodaj do koszyka</button>
        </div>
        {{-- Feature product loop end --}}
        {{-- Feature product loop start --}}
        <div class="feature__item">
            <a href="{{ $link }}" class="feature__link">
                <div class="feature__price">{{ $price }}</div>
                <img src="{{ $image }}" alt="" class="feature__image">
                <div class="feature__title">{{ $title }}</div>
                <div class="feature__desc">{{ $desc }}</div>
                <div class="feature__more"><span></span> Więcej informacji</div>
            </a>
            <button class="feature__cart">Dodaj do koszyka</button>
        </div>
        {{-- Feature product loop end --}}
        {{-- Feature product loop start --}}
        <div class="feature__item">
            <a href="{{ $link }}" class="feature__link">
                <div class="feature__price">{{ $price }}</div>
                <img src="{{ $image }}" alt="" class="feature__image">
                <div class="feature__title">{{ $title }}</div>
                <div class="feature__desc">{{ $desc }}</div>
                <div class="feature__more"><span></span> Więcej informacji</div>
            </a>
            <button class="feature__cart">Dodaj do koszyka</button>
        </div>
        {{-- Feature product loop end --}}
        {{-- Feature product loop start --}}
        <div class="feature__item">
            <a href="{{ $link }}" class="feature__link">
                <div class="feature__price">{{ $price }}</div>
                <img src="{{ $image }}" alt="" class="feature__image">
                <div class="feature__title">{{ $title }}</div>
                <div class="feature__desc">{{ $desc }}</div>
                <div class="feature__more"><span></span> Więcej informacji</div>
            </a>
            <button class="feature__cart">Dodaj do koszyka</button>
        </div>
        {{-- Feature product loop end --}}

    </div>
</div>